<div class="row ">
  <div class="large-3 columns show-for-large-up ">
    <div class= "container sidebar">
      <%= render 'shops/sidebar' %>
    </div>
    <div class= "container checkin-display">
      <%= render 'reviews/drink_checkin' %>
    </div>
  </div>  
  <div class="small-12 large-9 columns">
    <div class= "container information-display">
      <div class="row">
        <div class="hide-for-small-only medium-3 columns photo-item">
          <% if @drink.pictures.first %>
            <%= image_tag(@drink.pictures.first.photo.url(:medium), class: "th radius")  %>
          <% else %>
            <%= image_tag("http://placehold.it/300x300", class: "th radius")  %>
          <% end %>  
        </div>
        <div class="small-12 medium-9 columns">
          <h1><%= @drink.name %></h1>
          <p><%= @drink.description %></p>
          <span class="info-links center-on-small"> 
              <% if current_user %>
                <a href="#" data-reveal-id="addReview" class= "Radius button tiny primary"><i class="fa fa-plus fa-lg"></i> Add a new review</a>  
                <%= link_to '#', class: "button tiny primary", data: {"reveal-id" => "addPhoto"} do %>
                  <i class="fa fa-camera fa-lg"></i>
                <% end %> 
                <% @review = @drink.reviews.build %>
                <%= simple_form_for [@drink, @review]  do |f| %>
                  <%= f.hidden_field :review_type, value: :checkin%>
                  <%= f.hidden_field :shop_id, value: @shop.id %>
                  <%= button_tag(type: 'submit', class: "button tiny primary") do %>
                    <i class="fa fa-check-square-o fa-lg"></i>
                  <% end %>
                <% end %>
                <!-- <a href="#" data-reveal-id="addCheckin" class= " "></a>  -->
                <span class = "favorite-buttons">     
                  <% if @favorite.nil? || false %>
                    <%= render 'favorites/favorite_button' %>
                  <% elsif @favorite.favorable %>
                    <%= render 'favorites/unfavorite_button' %>
                  <% end %>
                </span>
              <% end %>
            </span>    
          </div> 
       </div>  
    </div> 
    <div class= "container information-display">
      <div class="row">
        <%= render 'pictures/display_photos' %>
      </div>
      <h4 class='activity_review'>Activity</h4>
      <hr class='margin_main_line'>
      <div class="row">
        <div class= "container activity-display">
            <%= render 'reviews/drink_reviews' %>    
        </div> 
    </div> 
  </div> 
</div>   

  <!-- Modal Divs -->
<% if current_user %>
  <div id="addReview" id="no_float" class="reveal-modal small" data-reveal>
    <%= render "reviews/form_reviews" %>
    <a class="close-reveal-modal">&#215;</a>
  </div> 
  <div id="addPhoto" class="reveal-modal small" data-reveal>
    <%= render "drinks/photo_upload" %>
    <a class="close-reveal-modal">&#215;</a>
  </div> 
<% end %>

