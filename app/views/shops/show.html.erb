<h1><%= @shop.name %></h1>
<h2><%= @shop.description %></h2>
  <!-- link to not quite working -->
<h2><%= link_to  @shop.website %></h2> 
  <%= link_to 'Back', shops_path %>
<div>

    <h3>Drinks</h3>
      <%= render 'shops/drinks' %>

    <%= link_to 'Add a drink', new_shop_drink_path(@shop) %> | <%= link_to 'Back', root_path %>
</div>
