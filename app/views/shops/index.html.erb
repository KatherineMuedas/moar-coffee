<h1>Caf&eacute;s</h1>
<%= link_to 'New Cafe', new_shop_path %>
<div>
  <ul>
    <% @shops.each do |shop| %>
      <li>
       <h4><%= shop.name %></h4>
       <p><%= shop.description %></p>
      </li> 
      <%= link_to 'Edit Cafe', edit_shop_path(shop.id) %> |
      <%= link_to 'Show Cafe', shop_path(shop.id) %>
    <%end%>
  </ul>
</div>
