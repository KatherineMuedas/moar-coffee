<h3> Edit Post </h3>
<%= form_for @post do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %>

  <%= f.label :discritpion%>
  <%= f.text_field :author %>

  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.submit %>
<% end %>
<%= render 'form' %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>